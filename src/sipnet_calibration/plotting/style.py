"""Roles and rcParams.

``ROLES`` maps a semantic role -- ``prior``, ``posterior``, ``obs``, ``truth`` --
to colour and line style, so that every figure in the project reads the same
way and style keywords stay out of call sites.
"""
